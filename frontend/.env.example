# Local development only. Production uses same-origin /api and /ws routes
# through Caddy, so these variables should be omitted from production builds.
NEXT_PUBLIC_API_ORIGIN=http://localhost:8000
NEXT_PUBLIC_WS_ORIGIN=ws://localhost:8000
