# Optional. When omitted, the browser's current hostname is used on port 8000.
NEXT_PUBLIC_API_ORIGIN=http://192.168.1.23:8000

# Optional. When omitted, this is derived from NEXT_PUBLIC_API_ORIGIN.
NEXT_PUBLIC_WS_ORIGIN=ws://192.168.1.23:8000
