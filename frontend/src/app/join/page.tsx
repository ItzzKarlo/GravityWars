import PrivateAccess from "@/components/PrivateAccess";

export default function JoinPage() {
  return <PrivateAccess title="PIN entry is closed" message="Lobby PINs are not access credentials. Ask the host for one of the three private invitation links." />;
}
